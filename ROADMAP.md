# План разработки: Этап 2 (Структура, CMS и Автоматизация)

## Завершенные задачи

- [x] **Шаг 1: Обновление модели данных (Frontmatter)**
  - Провести аудит всех файлов в `articles/unreal/`, `articles/blender/`, `articles/unity/` и `articles/cheats/`.
  - Добавить во все `.md` файлы поля: `title`, `module` (название раздела) и `order`.
  - Убедиться, что в корне каждой папки трека есть файл `intro.md` с глобальными настройками трека (`name`, `icon`, `colorClass`).

- [x] **Шаг 2: Внедрение и настройка Decap CMS**
  - Создать папку `admin/` в корне проекта.
  - Создать `admin/index.html` для подключения CMS.
  - Создать `admin/config.yml` с настройкой коллекций для:
    1. Учебных статей (с выбором трека и модуля).
    2. Шпаргалок.
    3. Настроек треков (intro.md).
    4. Портфолио проектов.
  - Настроить GitHub бэкенд для авторизации.

- [x] **Шаг 3: Настройка CI/CD (GitHub Actions)**
  - Создать директорию `.github/workflows/`.
  - Написать скрипт `build.yml`, который будет запускать `scripts/build_manifests.py` при пуше в `main` (или изменении файлов через CMS) и автоматически коммитить обновленные `.json` файлы.

- [x] **Шаг 4: Адаптация фронтенда (Отрисовка модулей)**
  - Обновить логику генерации бокового меню в `js/ui.js`, чтобы уроки группировались под заголовками `module`.
  - Обновить страницу "Треки" для блочного вывода модулей.
