# План разработки: Этап 2.1 (Багфикс и Стабилизация CMS)

## Завершенные задачи

- [x] **Шаг 1: Исправление конфигурации бэкенда и путей в CMS (`admin/config.yml`)**
  - Изменить `backend.name` на `git-gateway` (подготовка к деплою на Netlify).
  - Во всех коллекциях (Уроки, Проекты) заменить `slug: "{{title}}"` на `slug: "{{slug}}"`.
  - Добавить глобальную настройку `slug:` в корень конфига для транслитерации (чтобы русские буквы превращались в английские в URL).

- [x] **Шаг 2: Решение проблемы с фильтрацией `intro.md`**
  - Обновить конфигурацию коллекции Уроков в `config.yml`: вместо неработающего regex-фильтра добавить обязательное скрытое поле `type` со значением по умолчанию `lesson`.
  - Провести аудит `.md` файлов: убедиться, что в Frontmatter обычных уроков теперь есть `type: "lesson"`, а в `intro.md` — `type: "intro"`.

- [x] **Шаг 3: Рефакторинг фронтенда (Устранение дублирования кода в `js/ui.js`)**
  - Вынести логику группировки массива уроков по параметру `module` в отдельную вспомогательную функцию (например, `groupLessonsByModule(lessons)`).
  - Переписать функцию `renderTracks()`, чтобы она использовала эту новую вспомогательную функцию для отрисовки блоков модулей на странице треков.
  - Переписать функцию `buildLeftSidebar()`, чтобы она также использовала эту вспомогательную функцию для создания выпадающих списков модулей в боковом меню.
