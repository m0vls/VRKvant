# План разработки: Этап 2.3 (Автоматизация GitHub Actions)

## Завершенные задачи

- [x] **Шаг 1: Создание Workflow для автоматической сборки JSON**
  - Создать директорию `.github/workflows/` в корне проекта (если её нет).
  - Создать файл `.github/workflows/build.yml`.
  - Написать скрипт GitHub Actions, который будет:
    1. Срабатывать при каждом `push` в ветку `main`.
    2. Разворачивать окружение Ubuntu + Python 3.
    3. Запускать скрипт `scripts/build_manifests.py`.
    4. Проверять, изменились ли файлы `tracks.json`, `cheats.json` или `portfolio.json`.
    5. Если файлы изменились — автоматически делать коммит с сообщением "chore: auto-update JSON manifests" и пушить их обратно в `main`.
    6. Использовать `secrets.GITHUB_TOKEN` для прав на коммит.

# План разработки: Этап 2.4 (Переход на Sveltia CMS)

## Завершенные задачи

- [x] **Шаг 1: Обновление движка в `admin/index.html`**
  - Найти тег `<script src="https://unpkg.com/decap-cms@^3.0.0/dist/decap-cms.js"></script>`.
  - Заменить его на современный форк: `<script src="https://unpkg.com/@sveltia/cms/dist/sveltia-cms.js"></script>`.

- [x] **Шаг 2: Очистка бэкенда в `admin/config.yml`**
  - В блоке `backend` оставить ТОЛЬКО три параметра: `name: github`, `repo: NekrasovLE/VRKvant` и `branch: main`.
  - Полностью удалить строку `base_url: https://...` (так как Sveltia использует встроенную систему авторизации).

# План разработки: Этап 3 (Умный локальный поиск)

## Завершенные задачи

- [x] **Шаг 1: Добавление интерфейса поиска (HTML/CSS)**
  - В файле `index.html` добавить иконку лупы (`fas fa-search`) в верхнюю навигационную панель (рядом с переключателем темы).
  - В `index.html` сверстать скрытое модальное окно поиска (на весь экран с полупрозрачным фоном `backdrop-blur`), содержащее крупное поле ввода и блок для вывода результатов. Окно должно быть скрыто по умолчанию.

- [x] **Шаг 2: Создание логики поиска (`js/search.js`)**
  - Создать новый ES6 модуль `js/search.js`.
  - Написать функцию, которая принимает строку запроса и ищет совпадения (без учета регистра) по названиям уроков в `window.siteData.tracks`, шпаргалок в `window.siteData.cheats` и проектов.
  - Написать функцию для отрисовки результатов (каждый результат — это кликабельная ссылка, меняющая `window.location.hash`).

- [x] **Шаг 3: Интеграция и глобальные события**
  - Подключить `js/search.js` в главный файл `js/main.js`.
  - Прокинуть функции открытия/закрытия окна поиска в глобальную область видимости `window`, чтобы они работали по клику (onclick) в HTML.
  - Добавить слушатель клавиатуры: при нажатии `Escape` окно поиска должно закрываться.